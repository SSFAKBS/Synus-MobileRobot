<launch>
  <arg name="model" default="synus"/>
  <param name="model" value="$(arg model)"/>

  <!-- synus_teleop_key already has its own built in velocity smoother -->
  <node pkg="synus_teleop" type="synus_teleop_key" name="synus_teleop_keyboard"  output="screen">
  </node>
</launch>
